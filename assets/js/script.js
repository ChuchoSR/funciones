function pintar(ele, color){
    ele.style.backgroundColor = color;
    const text = document.getElementById('activeText').textContent = ' por el amarillo. Ya lo hiciste, recarga la pagina, que estas esperando? 😒';

    }
    


addEventListener('DOMContentLoaded', function() {
    const fondo = document.getElementById ('ele1');
    const text = document.getElementById('activeText').textContent = ' ' + 'verde';

    fondo.style.backgroundColor = 'green';
})